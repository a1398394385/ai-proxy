proxy:
  host: "127.0.0.1"
  port: 48743
  log_level: "INFO" # DEBUG / INFO / WARNING

ports:
  data_browser:
    host: "127.0.0.1"
    port: 18742
  codex_proxy:
    host: "127.0.0.1"
    port: 48743
  pass_through:
    host: "127.0.0.1"
    port: 48744

upstream:
  base_url: "https://llm-open-api.cargoware.com/v1"
  api_key: "sk-1f5H4uYz1mKZvEONUuQrQw"
  timeout: 1800 # SSE 流总超时（秒）
  connect_timeout: 60
  ssl_verify: false # 内网自签证书时可设为 false
  retry: 1 # 上游 5xx/超时时自动重试次数（0 = 不重试）
  proxy: "http://192.168.20.18:9090" # 上游请求代理（HTTP 格式，留空则直连）
  enable_proxy: false # 是否启用代理（true/false）。http.client 不读取环境变量，须显式路由

model_map:
  "codex-mini-latest":
    target: "kimi-k2.5"
    multimodal: true
  "o4-mini":
    target: "kimi-k2.5"
    multimodal: true
  "gpt-4o":
    target: "kimi-k2.5"
    multimodal: true
  "*":
    target: "kimi-k2.5"
    multimodal: true

logging:
  debug_retention_days: 7
  log_dir: "data"
  log_file: "access_log.db"

response_store:
  max_entries: 1000
  ttl_seconds: 3600
